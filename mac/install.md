# ## 🖥️ macOS Installation (Salishan Keyboard Layout)

### **1. Locate the layout file**

```
Salishan.keylayout
```

---

### **2. Install the layout file**

#### **Option A — Install for all users**

(Requires administrator permissions)

1. Open **Finder**
2. Press **⌘ + Shift + G**
3. Enter:

```
/Library/Keyboard Layouts/
```

4. Drag **Salishan.keylayout** from this folder into that one
5. Enter your admin password if prompted

---

#### **Option B — Install only for your user**

(No admin password needed)

1. Open **Finder**
2. Press **⌘ + Shift + G**
3. Enter:

```
~/Library/Keyboard Layouts/
```

4. Drag **Salishan.keylayout** into that folder
   (Create the folder if it doesn’t exist)

---

### **3. Enable the new keyboard layout**

1. Open **System Settings**
2. Go to **Keyboard → Input Sources**
3. Click **“Add Input Source…”**
4. Search for **Salishan**
5. Add it to your input sources

---

### **4. Log out and log back in**

This step ensures macOS loads the new keyboard layout.

---

### **5. Switch to the layout**

Use:

* The **input menu** in the menu bar
* Or the shortcut **Ctrl + Space**
